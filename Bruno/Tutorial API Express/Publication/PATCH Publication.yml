info:
  name: PATCH Publication
  type: http
  seq: 10

http:
  method: PATCH
  url: http://localhost:3000/publication/7
  body:
    type: json
    data: |-
      {
        "title": "Produto inteligente"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
