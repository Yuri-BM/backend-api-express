info:
  name: UPDATE Publication
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:3000/publication/4
  body:
    type: json
    data: |-
      {
        "title": "Minha certificação EF",
        "description": "Cerificação de inglês",
        "authorId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
