info:
  name: PATCH Publication
  type: http
  seq: 10

http:
  method: PATCH
  url: http://localhost:3000/publication
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
