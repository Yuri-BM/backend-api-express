info:
  name: POST Publication
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/publication
  body:
    type: json
    data: |-
      {
        "title": "Minha certificação",
        "description": "Certificação de inglês do Yúri",
        "authorId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
