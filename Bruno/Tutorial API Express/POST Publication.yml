info:
  name: POST Publication
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/publication
  body:
    type: json
    data: |-
      {
        "title": "Yúri Post",
        "description": "Post do Yúri",
        "authorId": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
