info:
  name: POST User
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/user
  body:
    type: json
    data: |-
      {
        "name": "Pedro",
        "email": "pedro@example.com",
        "pass": "securepasswordpedro",
        "avatar": "https://example.com/avatarpedro.jpg"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
